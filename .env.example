# Environment Variables for OpenHuman Email Client
# Copy this file to .env and fill in the actual values

# Vite environment variables (must be prefixed with VITE_)
# These are exposed to the frontend code

# API URL (example - not used yet)
VITE_API_URL=

# Environment mode (development, staging, production)
VITE_ENV=development

# Feature flags (example - not used yet)
VITE_ENABLE_DEBUG_MODE=false

# Backend environment variables
# Add Tauri/Rust backend environment variables here as needed
